# Horizon FMS - Environment Configuration
# Copy this file to .env to override defaults

# --- Docker Compose Ports ---
# Change these if you have port conflicts
API_PORT=5295
WEB_PORT=8080
DB_PORT=5432
REDIS_PORT=6379
SEQ_PORT=5341

# --- Docker Services ---
POSTGRES_USER=horizon_user
POSTGRES_PASSWORD=horizon_password
POSTGRES_DB=horizon_fms

# --- API Configuration ---
# These are used inside the API container
ASPNETCORE_ENVIRONMENT=Development
ConnectionStrings__DefaultConnection=Host=database;Port=5432;Database=horizon_fms;Username=horizon_user;Password=horizon_password;
SEQ_URL=http://seq:5341
REDIS_CONNECTION=redis:6379

# --- CORS Configuration ---
# Comma-separated list of allowed origins for production
# Example: AllowedOrigins__0=https://myapp.com
# Example: AllowedOrigins__1=https://app.myapp.com
# Local development origins (localhost:3000, localhost:5173) are always allowed
